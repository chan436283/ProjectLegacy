// Serialized values are explicit so adding entries does not change existing assets.
public enum SkillAnimationType
{
    None = 0,
    Attack = 1,
    Cast = 2,
    DefendStart = 3,
    DefendLoop = 4,
    DefendEnd = 5
}
